{
  "name": "revival-bible-studio",
  "version": "0.0.1",
  "description": "Local-first creative/editorial workspace for the Revival project.",
  "private": true,
  "main": "main.js",
  "scripts": {
    "dev": "electron .",
    "rebuild": "electron-rebuild -f -w better-sqlite3",
    "postinstall": "electron-rebuild -f -w better-sqlite3"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.4",
    "electron": "^31.0.0",
    "playwright-core": "^1.60.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  }
}
